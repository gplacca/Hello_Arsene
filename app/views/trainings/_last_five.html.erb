<h2>Mes dernières séances</h2>

<% @last_five_trainings.each do |training| %>
    <p><%= training.name %> <%= link_to "Modifier", edit_training_path(training.id) %> <%= link_to "Voir", training_path(training.id) %></p>

<% end %>
